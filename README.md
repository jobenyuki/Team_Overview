# Professor dashboard

# How to install
Run locally: `yarn` && `yarn dev`
Build: `yarn build`

# How to run storybook
Run: `yarn storybook`
Build: `yarn build-storybook`

# How to run test
`yarn test` or `yarn test:watch`